import { Navigation } from "../../components/Navigation";
import "./style.scss";

export const Header = () => (
  <header>
    <div className="container">
      <Navigation />
    </div>
  </header>
);
